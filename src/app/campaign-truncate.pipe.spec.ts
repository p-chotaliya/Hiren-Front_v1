import { CampaignTruncatePipe } from './campaign-truncate.pipe';

describe('CampaignTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new CampaignTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
