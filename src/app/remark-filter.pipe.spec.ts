import { RemarkFilterPipe } from './remark-filter.pipe';

describe('RemarkFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RemarkFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
